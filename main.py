from network import Network
from layer import Layer
from node import Node

# Define network
network = Network()

# Define layer
layer = Layer()
node = Node()
layer.add_node(node)
layer.add_node(node)
layer.add_node(node)

# Build network
network.add_layer(layer)



